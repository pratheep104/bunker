// Demo data removed - BUNKER now uses real PSG eCampus integration
// All attendance data is fetched live from the backend service

import type { TimetableEntry } from '../types';

// Empty timetable for initial state
export const EMPTY_TIMETABLE: TimetableEntry[] = [];
